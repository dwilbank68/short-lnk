import React, { PropTypes } from 'react';

const NotFound = (props) => {

    return (
        <div className="not-found">
            Not Found (sfc)
        </div>
    );
};

export default NotFound;